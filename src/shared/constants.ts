/**
 * Shared constants to avoid magic values across features.
 */

/** Duration in ms to show "Copied!" feedback after copy-to-clipboard. */
export const COPY_FEEDBACK_MS = 1500

/** Max duration in seconds for the builder duration slider (0 = persistent). */
export const MAX_DURATION_SEC = 10
